const _ = require("lodash");

const a = [1, 2, 3, 4, 5];
const b = [2, 8, 9, 7, 0];

const diff = _.difference(a, b);

console.log(diff);

//procurando arquivos depreciados com o comando(npx npm-check-updates -u)
